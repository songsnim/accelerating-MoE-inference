# build artifacts
obj/
main
main[0-9]*

# run outputs and local references
*.bin
*.log

# environment / vendored installer
.venv/
get-pip.py

# pre-git backup of the editable files (superseded by the baseline commit)
.baseline/
outputs/

# local agent worktrees
.claude/
