# 실험 계획

기준: EXP-015 완료. b0 기준 **2.174 s / 472.2 seq/s**.
제출 기록은 EXP-012 시점 **408.4 seq/s**.
남은 시간 분해(b0): **gemm 1.096 (50.4%)** · moe 0.698 (32.1%) · attn 0.144 · norm 0.113.
근거: `exploration-003.md`(2위 라인 대조) + EXP-008 ncu 실측 + `moeoptimizationlog.html` 대조.

## 순서

| # | 실험 | 기대 | 근거 |
|---|---|---|---|
| ~~009~~ | **Attention 직렬 `exp` 분산** (완료 `cd3cdf6`) | **−0.109 s 실측** | attn 0.291 → 0.183. 직렬 구간을 통째로 삭제한 하한이 0.178 s → 가용분의 96% 회수, 이 방향 소진. bitwise 동일 |
| **010** | **MoE W1/W3 fusion + grouped GEMM** — N=896(=7×128) 융합, expert를 `blockIdx.z` | −0.2~0.37 s | ncu 실측: 블록 40~72 / 164 슬롯(24~44% 점유) + N=448에 BN=128이라 **12.5% 패딩 낭비**. EXP-008이 만든 moe 퇴행(1.259→1.398) 상환. 누산 순서 불변 → `cmp` 판정 |
| ~~011~~ | **Prefix trie** (완료 `47aff75`) | **−0.628 s 실측** | 19,803 → 15,583 행. elapsed 3.281 → 2.653 (b5), bitwise 동일. attn은 `qi` 직렬 루프 소멸로 −24.7% |
| **012** | **expert w13 grouping** — (expert, rowtile) work queue | −0.19 s 이상 | 측정-A: w13 시간가중 wave 충전율 66%, 53%가 1 wave 미만 launch. trie로 expert당 행이 21% 줄어 충전율은 더 나빠졌다 |
| ~~013~~ | **embedding lookup 디바이스 이전** (완료) | **−0.23 s 실측** | embed 0.181 + h2d 0.023 → 0.002 + 0.004. bitwise 동일. trie 구성 자체는 2 ms로 확정 |
| ~~014~~ | **출력 Tensor 할당 겹치기** (완료 `9ccf876`) | **−0.068 s 실측** | `Tensor` 생성자가 131 MB를 `assign(0.0f)`로 페이지폴트 채우는 0.069 s. GPU 의존 없음 → 별도 스레드. bitwise 동일 |
| ~~015~~ | **device-side routing** (완료 `0db90c5`) | **−0.071 s 실측** | 호스트 왕복 0.060 + memset 8.2 GB 제거. moe 0.780→0.698, d2h 0.005→0.002. bitwise 동일. gemm은 boost 클럭으로 +0.012 |
| **016** | **attention 청크 스테이징 (D3)** — key 행을 32열씩 shared로 | **−0.041 s 실측 (EXP-013 코드 기준)** | 측정-C·coalescing(`961c1e3`, 미머지 브랜치). attn 0.143 → 0.096, bitwise 동일, 코드 형태(§3)까지 확정. `attention_heads`가 main과 **바이트 동일**이라 그대로 이식된다. 017과 다른 커널이라 상호작용 0 |
| **017** | **GEMM `bs` 뱅크 충돌 제거** — `tx*8` → `tx*4` + `tx*4 + BN/2` | ? (아래 §로그 대조 A) | **최대 레버.** gemm 1.096 + moe 0.698 = **1.79 s (82.5%)**에 동시 적용 |
| 018 | **bias 에필로그 호이스팅** — `bv[TN]`을 행 루프 밖에서 8회 | ? (로그 실측 기준 ≈+3%) | SASS 실측: `gemm_nt_bias` LDG **68** vs `gemm_grouped` **8**. 차 60 = 스레드당 `bias[col]` 64회. 대상 gemm 1.096 |
| 019 | **gate 전용 tall-skinny 커널** | −0.03 s 이하 | gate가 `gemm_nt_bias`에 `out=16`. grid.x=1이고 BN=128 타일 중 **16열만 유효 → FFMA·bs 트래픽 87.5% 폐기** |
| **020** | **스크래치 버퍼 수명을 모델로** — `generate()`의 `DeviceBuffer` 13개를 멤버로 올려 `cudaFree`를 소멸자로 | **−0.021 s 상한 실측** | 측정-E: 에필로그 `cudaFree` 24회 ~1.5 GB가 측정 구간 안에서 **20.77 ms(0.96%)**, GPU idle. D2H 전체의 1.45배. `cudaMalloc`은 구간 안에 그대로 둬 워밍업 캐싱 논점 없음. 산술 무변경 → bitwise 동일 |
| 이후 | LayerNorm fused residual −0.10 · GEMM 타일 재설계(017 이후 재판단, 아래 참조) | | |

## `moeoptimizationlog.html` 대조 (v0~v18)

로그의 19개 버전을 우리 코드와 1:1로 대조했다. **이미 확보한 것**: 디바이스 상주(v1~v5)
· 순차 누산 정확도(v6) · 레지스터 블로킹(v7, 우리는 8×8) · 디바이스 임베딩(v8=013)
· `float4` 벡터 로드(v11 전반) · RoPE LUT(v12) · expert grouping(v13=012)
· softmax `exp` 분산(v14=009) · prefix trie(v15=011) · LayerNorm 리덕션/맵 분리(v16)
· 더블 버퍼링(v17) · LayerNorm 블록당 1행 shared 스테이징(v18①).

v17의 `__launch_bounds__(256,2)` 레버는 **이미 회수된 상태**다 — `gemm_nt_bias`와
`gemm_grouped` 둘 다 `ptxas -v` 실측 **127 레지스터**(≤128)이고 smem 16,896 B라
SM당 2블록이 이미 나온다. 붙여도 이득 없음(회귀 방지용으로는 유효).

**미적용은 셋뿐이고, 그것이 016~019다.**

### A. `bs` 뱅크 충돌 (로그 v11) — 017

우리 K 루프는 `&bc[p][tx * TN + j]`, j=0/4 → **워드 stride 8**이다. 로그가 "이전"으로
적어둔 바로 그 패턴이다. LDS.128은 quarter-warp(8스레드) 단위로 처리되는데 tx=0..7이
뱅크 {0–3, 8–11, 16–19, 24–27}만 덮어 **2중 충돌**(로그의 half-warp 뷰로는 4중)이다.

**`SPAD=4`는 이 문제를 못 고친다.** 베이스가 `p*132`라 뱅크가 p마다 4씩 회전할 뿐
8스레드의 stride-8 패턴은 그대로다. 로그도 정확히 이걸 기각표에 올려놨다 —
"반사적으로 떠올린 패딩 해법은 저장부만 고치고 뜨거운 읽기 루프는 못 고쳤다"(+1.2%, 느림).

- **수정**: `bc[p][tx*4]` + `bc[p][tx*4 + BN/2]`. 8스레드가 32뱅크를 정확히 한 번씩 덮는다.
- **`as`는 손대지 않는다** — quarter-warp 안에서 `ty`가 상수라 같은 주소 브로드캐스트다.
- **누산 순서 불변**: 스레드↔출력 열의 *대응*만 바뀐다. 각 출력은 여전히 같은 k 오름차순
  FMA 열을 본다 → `cmp` bitwise 동일이 판정 기준.
- **상한 근거**: 로그의 균형식은 k-step당 `4 × LDS.128 = 2048 B ÷ 128 B/clk = 16 사이클`
  = `FFMA 64회 ÷ 4/clk = 16 사이클`이다. 여기에 `bs` 2중 충돌을 넣으면 LDS가 24사이클로
  FFMA를 **1.5× 초과**한다. 측정-C의 우리 ncu와도 일관: `gemm_grouped` l1 **74.0%** >
  sm 53.0%, `gemm_nt_bias` l1 **55.2%** > sm 39.4% — 공유 파이프가 더 포화한 쪽이다.
- **먼저 할 것**: 구현 전에 ncu 카운터
  `l1tex__data_bank_conflicts_pipe_lsu_mem_shared_op_ld.sum` 1회로 런타임 충돌을 확인한다.
  정적 분석만으로 착수하지 않는다.

### B. bias 에필로그 호이스팅 (로그 v18②) — 018

SASS 실측으로 미적용이 확정됐다: `gemm_nt_bias` LDG **68** vs `gemm_grouped` LDG **8**.
차이 60개가 스레드당 `bias[col]` 64회 로드다. `if (row >= m) continue;` 때문에
컴파일러가 행 루프 밖으로 못 뺐다.

로그가 기록한 실패-반전 사례와 **정확히 같은 상태**다 — 저쪽도 이 형태에서 −1.7%였고
행 루프 밖으로 빼서 +1.53%로 뒤집혔다. `float bv[TN]` 8개를 미리 읽으면 되고 산술 무변경.
017이 열 매핑(`col = n0 + tx*TN + j`)을 바꾸므로 **017 다음에, 새 매핑을 따라** 넣는다.

### C. gate tall-skinny (로그 v18③) — 019

로그의 `[15583,4096] × [16,4096]`과 형상이 같다. 출력이 1 MB인데 A가 255 MB라 재사용할
것이 없고 타일링할 이유도 없다. 로그 실측 55.4 → 35.3 ms. 우리 gate는 측정-A에서
0.050 s(b1)라 상한이 작다 — 017/018 다음.

### 로그가 죽여주는 것

**`GEMM 타일 재설계`를 016에서 내렸다.** 로그 기각표가 `128×256`(+7% 느림)과
`256×64` 16×4 마이크로타일(**+93% 느림**)을 실측했고, 규칙 절이 이유까지 준다 —
현재 `128×128×8 / 8×8`에서 공유 대역폭과 FFMA 처리량이 **1:1로 묶여** K 루프에 아무것도
더 넣을 수 없다. **017이 그 1:1을 비로소 성립시킨다.** 017 없이 타일만 키우면 충돌이
같이 확대된다. 재판단은 017 실측 이후.

**앞으로 GEMM에 무언가를 융합할 때의 판정 기준** (로그 규칙 절): *에필로그에 들어가면
가능(출력당 1회), K 루프에 들어가면 불가.* 018이 되는 이유이자 저쪽의 gather 제거가
실패한 이유다.

### 대조에서 나온 정합성 확인

로그의 최종 487 seq/s는 우리 현재 472.2와 **할당 간 편차(로그 자체 기준 최대 15.7%)
안**이다. 그리고 로그가 우리보다 앞선 부분은 위 A·B·C 셋으로 정확히 떨어진다 —
"저쪽이 기법 6개 앞서 있는데 총시간은 우리가 빠르다"는 미해결 항목이 여기서 상당 부분
설명된다. 다만 `moeoptimizationlog.html`이 우리 라인(trie·grouping·device embedding)을
포함하고 있어 **저쪽 단독 기록인지 합본인지는 미확인**이다. 수치의 절대값이 아니라
기법 목록으로만 쓴다.


## 하지 않는 것

| 항목 | 사유 |
|---|---|
| **Q/O `cp.async` 2-stage** | 현재 타일이 `as[BK][BM]` **전치** 레이아웃이라 `cp.async`(연속 복사)로 채울 수 없다. 전치를 버리면 내부 루프 shared 로드가 `LDS.128` 4개 → `LDS.32` 16개. 타일 재설계와 묶어야 한다 |
| **Tensor Core (전 범위)** | 정확도 사망. split 3항·4항 모두 0.29916 FAILED이고 **4항을 넣어도 오차 불변** — 원인은 표현 절삭이 아니라 WMMA accumulator 연산 순서라 operand splitting으로 회생 불가. 합법 범위(lm_head + layer 31)는 실측 15 ms(0.35%) |
| **CUDA Graph / launch 수 감소** | 독립 측정 2건 동일 결론. 저쪽 610 gap 합 2 ms(0.05%), 우리 3969 gap 합 8 ms |
| **feature-major 레이아웃 전환** | ncu 실측: sectors/request 14.5~15.8(이상 16), dram_throughput 3.7~10.8%. 메모리 경로에 고칠 것 없음. 대가는 전 커널 재인덱싱 |
| **attention key 방향 warp 병렬화** | 저쪽이 n=1024에서 실측, **이득 없음(잡음 수준)** |
| **attention Q-head 4-way (3.49x)** | 적용 대상 없음. 저쪽 3.49x는 GQA grouping으로 head 4개를 한 블록에 묶으며 생긴 페널티를 되돌린 것 — 우리는 묶지 않아 그 페널티가 없다. exploration-003 §2가 스스로 철회, EXP-009 실측이 독립 확인 |
| **트리플 버퍼링 (H2D / kernel / D2H 3-stream)** | 측정-E. 측정 구간 안의 버스 트래픽 전량이 **7건 14.37 ms(0.66%)**이고 H2D는 1 MB / **0.063 ms(0.003%)** — 가중치 15.0 GB는 전부 생성자다. 파이프라인에 태울 반복 흐름이 없다(1 MB trie 메타는 32레이어가 공유해 청크 불가, logits는 32레이어 후 1회). 커널 윈도우 idle도 1.70 ms(0.08%)라 compute 스트림도 메울 틈이 없다. 겹치기 대상은 꼬리 D2H 하나, 상한 0.66% |
| **logits D2H를 pinned로** | 측정-C·pinned(`c4ec9d5`). 131 MB D2H 15 ms를 pinned로 줄이려면 (a) `cudaHostRegister` 13 + 5.8 ms 또는 (b) pinned ring + 청크 파이프라인. 둘 다 실이득 **10~12 ms(0.5%)**인데 `cudaHostAlloc`이 0.83 ms/MB, 청크 GEMM·비블로킹 스트림·이벤트·memcpy 스레드가 붙는다. `Tensor`가 `std::vector`라 할당기를 못 바꾸는 게 근본 제약 |
| 병렬 LayerNorm reduction · warp dot reduction · 병렬 product+순차 sum | 전부 FP32 누적 순서 변경 → MoE top-2 routing 붕괴. EXP-004(0.211975)와 저쪽(0.211973) 상호 검증 |
| **QKV(K/V) projection 융합** | 로그 기각표 실측 **0%**. "A를 3번이 아니라 1번만 스트리밍"은 맞지만 이득이 없다. 기존 `이후` 행에서 삭제 |
| **L2 스레드블록 스위즐** | 로그 기각표 실측 **0%**. CUTLASS 표준 기법이지만 이 형상에서는 무이득 |
| **GEMM 경계 검사 제거** | 로그 기각표 실측 **+3.9% 느림** |
| **직사각/저(低)스레드 타일 (`128×256`, `256×64`)** | 로그 기각표 실측 **+7% / +93% 느림**. 512스레드 블록이 되면 SM당 1블록으로 떨어져 실효 대역폭 692 → 489 GB/s |
| **MoE gather 커널 제거 (행 맵을 레지스터/공유메모리로)** | 로그 기각표 실측 **−4.3% / −2.5%**(둘 다 느림). 행 맵이 **K 루프**에 들어가기 때문 — 위 융합 판정 기준 |
| **shared 패딩만 늘려 뱅크 충돌 해결** | 로그 기각표 실측 **+1.2% 느림**. 저장부만 고치고 읽기 루프를 못 고친다. 017은 패딩이 아니라 **읽기 인덱스**를 바꾸는 것 |

## 측정 규율

- **A/B는 하나의 `srun` 할당 안에서 교차 실행.** 노드 편차 1.19x가 개선폭보다 크다.
  EXP-015 시점 동일 바이너리 실측: b0 2.159 · b5 2.154 · b6 2.163 (fast) vs
  b1 2.580 · b7 2.591 (slow). **1.196x.** `run.sh`에 노드 지정이 없어 제출은 추첨이다
  (EXP-015 제출 2회 모두 slow에 걸려 396 seq/s로 기록, 실제는 472).
- **연산 순서 변경 여부는 오차값이 아니라 출력 binary `cmp`로 판정.** 저쪽 통과 제출이
  max abs 0.00369644(abs 임계 초과, relative로 통과)이고 동일 binary가 노드에 따라 17배
  차이를 보인다 — 오차값은 안정된 양이 아니다.
- **최종 판정은 n=1024.** n=64 통과가 n=1024 통과를 보장하지 않는다(저쪽 실측).
- 실험 1개 = 가설 1개 = 커밋 1개.

## 미해결

- 저쪽 체크아웃은 이 머신에 없다. 접근 가능해지면 **6개 커널을 우리 쪽에 재구현하는 것보다
  trie를 저쪽에 이식하는 편이 훨씬 싸다** — 저쪽은 6개 앞서 있고 우리가 더할 것은 trie 하나다.
- ~~남은 attn의 내역(score dot 대 value 누적)은 미분해~~ → **측정-C·coalescing에서 분해 완료.**
  섹터의 88.9%가 score dot이고 천장은 0.074 s, 실현분 0.047 s(=016).
- **`측정-C` 이름이 충돌한다.** `main`의 측정-C는 *pageable 전송/pinned 상한*(`c4ec9d5`)이고,
  `exp013-coalescing` 브랜치의 측정-C는 *memory coalescing 여유*(`961c1e3`)다. 두 브랜치가
  lab-note 같은 위치에 각각 추가했다. **016을 커밋할 때 후자를 `측정-D`로 개명해 main에
  병합할 것.** 그 브랜치는 EXP-014/015를 포함하지 않으므로 lab-note 섹션만 옮기고
  코드는 main 위에 다시 얹는다.
- 016의 −0.041 s는 EXP-013 코드에서 잰 값이다. main은 EXP-015로 호스트 대기가 사라져
  boost 클럭이 내려간 상태라(측정-B/EXP-015 §4) **재측정 필요**. 방향과 bitwise 동일성은
  유효하다.
- ~~저쪽이 기법 6개 앞서 있는데 총시간은 우리가 빠른 이유가 미해명~~ → **상당 부분 설명됨.**
  `moeoptimizationlog.html` 대조 결과 우리에게 없는 기법은 A·B·C 셋(017~019)뿐이고,
  나머지는 이미 확보했거나(v16·v17·v18①) 우리가 다른 경로로 해결했다(v9 형상별 타일 →
  grouping). 위 「대조에서 나온 정합성 확인」 참조.
- `moeoptimizationlog.html`의 출처가 미확인이다 — 우리 라인의 trie·grouping·device
  embedding을 포함하고 있어 저쪽 단독 기록인지 합본인지 모른다. **기법 목록으로만 쓰고
  절대 수치는 우리 A/B 재측정으로 대체한다.**
- 측정 섹션 문자: main에 `측정-E`(트리플 버퍼링 판단)가 추가됐다. 016 병합 시 coalescing 브랜치의 `측정-C`는 **`측정-D`로** 개명해 넣는다(비어 있는 문자).
